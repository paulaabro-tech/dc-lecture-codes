// Faça um programa que verifica se uma pessoa é brasileira ou não. // DEVE SER UM OBJETO

const person = {
  name: "Paula",
  nationality: "brazilian"
};

if (person.nationality === "brazilian") console.log("BRAZILIAN!");
else console.log("NOT brazilian!");
